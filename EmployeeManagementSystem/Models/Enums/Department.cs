namespace EmployeeManagementSystem.Models.Enums;

// Simple enum (removed Description attribute to avoid extra using)
public enum Department
{
    HumanResources,
    Engineering,
    Marketing,
    Finance,
    Operations
}